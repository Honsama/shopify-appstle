# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Vercel
.vercel/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Shelf Digest — subscriber PII + send log, never commit
digest/digests.json
digest/send-state.json
digest/samples/
digest/box-history.json
